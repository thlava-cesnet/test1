
name: Matrix test 2

on:
  workflow_call:
    inputs:
      oarepo:
        description: OARepo version (11, 12, ...)
        required: true
        default: 11
        type: string
  workflow_dispatch:
    inputs:
      oarepo:
        description: OARepo version (11, 12, ...)
        required: true
        default: 11

jobs:
  get-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: set matrix
        id: set-matrix
        run: echo 'matrix={"include:":[{"library":"oarepo/oarepo-runtime"},{"library":"oarepo/oarepo-upload-cli"},{"library":"Narodni-repozitar/nr-model"}]}' >> $GITHUB_OUTPUT

  print:
    runs-on: ubuntu-latest
    needs: get-matrix
    strategy:
      matrix: ${{ fromJson(needs.get-matrix.outputs.matrix) }}
    env:
      OAREPO_VERSION: ${{ github.event.inputs.oarepo }}
      TOK: ${{ secrets.OAREPO_BOT }}
    steps:
      - name: Print output
        run: |
          echo "Output: ${{ matrix.library }}"
